import { Ball } from "./Ball";
import { Player } from "./Player";
export interface Room {
    id: string;
    ball: Ball;
    leftPlayer: Player;
    rightPlayer: Player;
}
//# sourceMappingURL=Room.d.ts.map